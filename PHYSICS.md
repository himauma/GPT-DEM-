# DEM Physics Documentation

## 離散要素法（Discrete Element Method）の物理原理

### 概要

離散要素法（DEM）は、粒状物質や粒子システムをシミュレートするための数値解析手法です。各粒子を個別に追跡し、粒子間の相互作用を計算します。

## 基本方程式

### 運動方程式

各粒子の運動は、ニュートンの第二法則に従います：

```
F = m·a
```

ここで：
- F: 粒子に作用する力の合計
- m: 粒子の質量
- a: 粒子の加速度

### 時間積分

位置と速度は、明示的オイラー法で更新されます：

```
v(t+Δt) = v(t) + a(t)·Δt
x(t+Δt) = x(t) + v(t)·Δt
```

## 力のモデル

### 1. 重力

すべての粒子に常に作用する力：

```
F_gravity = m·g·ĵ
```

ここで：
- g: 重力加速度（デフォルト: 9.81 m/s²）
- ĵ: y方向の単位ベクトル（下向きを負とする）

### 2. 接触力（粒子間）

2つの粒子が接触している場合、以下の力が作用します：

#### 法線力（Normal Force）

スプリング-ダンパーモデルを使用：

```
F_n = k·δ - γ·v_n
```

ここで：
- k: 接触剛性（stiffness）
- δ: 重なり量 = (r₁ + r₂) - d
- r₁, r₂: 粒子の半径
- d: 粒子中心間の距離
- γ: 減衰係数（damping coefficient）
- v_n: 法線方向の相対速度

#### 接線力（Tangential Force）

クーロン摩擦モデルを使用：

```
F_t = -μ·|F_n|·sign(v_t)
```

ここで：
- μ: 摩擦係数（friction coefficient）
- v_t: 接線方向の相対速度

### 3. 壁との接触力

壁との接触は、無限質量の粒子との接触として扱われます：

```
F_wall = k·δ_wall - γ·v_normal
```

#### 底面との摩擦

粒子が底面と接触している場合、追加の摩擦力が作用します：

```
F_friction = -μ·|F_n|·sign(v_x)
```

## パラメータの影響

### 反発係数（Coefficient of Restitution）

反発係数 e は、衝突後のエネルギー保存を決定します：

```
e = |v_after| / |v_before|
```

- e = 0: 完全非弾性衝突（エネルギーが完全に失われる）
- e = 1: 完全弾性衝突（エネルギーが保存される）
- 0 < e < 1: 部分的にエネルギーが失われる

### 剛性（Stiffness）

剛性 k は、粒子の硬さを表します：

- **小さい値（k < 10⁴）**: 柔らかい粒子、大きな変形
- **中程度の値（10⁴ < k < 10⁶）**: 一般的な粒状物質
- **大きい値（k > 10⁶）**: 非常に硬い粒子、小さな変形

注意：剛性が大きいほど、安定したシミュレーションのために小さな時間刻みが必要になります。

### 減衰係数（Damping Coefficient）

減衰係数 γ は、エネルギー散逸を制御します：

- **γ = 0**: 減衰なし（完全弾性）
- **0 < γ < 1**: 通常の減衰
- **γ > 1**: 過減衰（振動が抑制される）

### 摩擦係数（Friction Coefficient）

摩擦係数 μ は、接線方向の力を決定します：

- **μ = 0**: 摩擦なし（完全滑り）
- **0.1 < μ < 0.3**: 低摩擦（氷、ガラスビーズ）
- **0.3 < μ < 0.6**: 中程度の摩擦（砂、土）
- **μ > 0.6**: 高摩擦（ゴム）

## 時間刻みの選択

安定したシミュレーションのために、時間刻み Δt は以下の条件を満たす必要があります：

```
Δt < π·√(m/k)
```

これは、接触の振動周期よりも十分小さい時間刻みを使用することを意味します。

推奨される時間刻み：
- 柔らかい粒子（k ≈ 10⁴）: Δt ≈ 0.001秒
- 硬い粒子（k ≈ 10⁵）: Δt ≈ 0.0005秒
- 非常に硬い粒子（k ≈ 10⁶）: Δt ≈ 0.0001秒

## エネルギー保存

理想的な弾性系では、全エネルギーは保存されます：

```
E_total = E_kinetic + E_potential
E_kinetic = Σ(½·m·v²)
E_potential = Σ(m·g·h)
```

実際のシミュレーションでは：
- 減衰により運動エネルギーが失われる
- 数値誤差により多少のエネルギー変動がある
- 時間刻みが大きすぎると、エネルギーが増加することがある（数値不安定性）

## 実装の詳細

### 衝突検出

各時間ステップで：
1. すべての粒子ペアについて距離を計算
2. 距離 < (r₁ + r₂) の場合、衝突が発生
3. 接触力を計算して適用

計算量：O(N²)、ここでNは粒子数

### 力の更新順序

各時間ステップで：
1. すべての粒子の力をゼロにリセット
2. 重力を適用
3. 粒子間の接触力を計算
4. 壁との接触力を計算
5. 加速度、速度、位置を更新

## 応用例

### 粒子の落下

単一または複数の粒子が重力により落下し、底面に着地します。
- 反発係数が高い場合、粒子は何度もバウンドします
- 反発係数が低い場合、粒子はすぐに停止します

### 粒子の山形成

複数の粒子を落下させると、底面に粒子の山が形成されます。
- 摩擦係数が高いほど、急な斜面が形成されます
- 摩擦係数が低いほど、平坦な山が形成されます

### 粒子の流動

粒子を容器内で混合すると、粒状物質の流動挙動を観察できます。

## 参考文献

1. Cundall, P. A., & Strack, O. D. (1979). "A discrete numerical model for granular assemblies". Geotechnique, 29(1), 47-65.

2. Zhu, H. P., Zhou, Z. Y., Yang, R. Y., & Yu, A. B. (2007). "Discrete particle simulation of particulate systems: Theoretical developments". Chemical Engineering Science, 62(13), 3378-3396.

3. Pöschel, T., & Schwager, T. (2005). "Computational Granular Dynamics: Models and Algorithms". Springer.
